# Audit log — uj-claudex-loop — 2026-08-30

Reviewer output verbatim, plus the dispositions. The prioritised deliverable is
[`2026-08-30-baseline.md`](./2026-08-30-baseline.md); this file is the evidence behind it.

## Actors (resolved, not assumed)

`python scripts/claudex_roles.py --explain` → exit 0, `gates OK`, config = built-in
defaults (no `.claudex.yaml` in this repo).

| Role | Actor | Model / effort | Sandbox |
|---|---|---|---|
| `audit` | codex | `gpt-5.6-terra` / high | read-only |
| `exposure-review` | codex | `gpt-5.6-sol` / medium | read-only |

`producer_never_reviews: True`, `adversary_read_only: True`.

## Resolved tunables

| Var | Value |
|---|---|
| `SLICES` | 7 (see below) — confirmed by the owner, `legacy/` added on request |
| `DIMENSIONS` | `security,quality,docs,tests,rules` |
| `SEVERITY_FLOOR` | `low` |
| `EXPOSED` | egress boundary only (see Step 3b) — confirmed by the owner |
| `BASELINE_FILE` | `docs/audit/2026-08-30-baseline.md` |
| `LOG_FILE` | this file |

## Step 1 — slices and coverage

| # | Slice | Files |
|---|---|---|
| S1 | wrapper | `scripts/codex_ro.py`, `scripts/wrapper_drift.py`, `tests/test_codex_ro.py` |
| S2 | roles | `scripts/claudex_roles.py`, `.claudex.yaml.example`, `tests/test_roles.py`, `ROLES.md` |
| S3 | fallback | `scripts/fallback_review.py`, `scripts/codex_usage.py`, `.env.example`, `tests/test_egress.py`, `tests/test_autoload.py`, `FALLBACK.md` |
| S4 | hooks | `hooks/wrapper_guard.py`, `hooks/claudex-python.sh`, `hooks/hooks.json`, `tests/test_wrapper_guard.py` |
| S5a | skills core | `skills/claudex-loop`, `skills/build`, `skills/code-review`, `skills/audit` |
| S5b | skills rest + docs | `skills/plan-review`, `skills/docs-backfill`, `skills/setup`, `README.md`, `docs/betrieb.md`, `.claude-plugin/*.json`, `hooks/hooks.json` |
| S6 | legacy | `legacy/grill-me-codex/*`, `legacy/grill-with-docs-codex/*` |

**NOT audited:** `assets/*.svg`, `LICENSE`, `docs/self-review/` (existing review logs),
`.pytest_cache/`, `__pycache__/`, and this `docs/audit/` directory itself.

### Exposure map (Step 1)

| Component | How it is reached | Login? |
|---|---|---|
| *(none inbound)* | — | — |
| Egress: `fallback_review.py` → LM Studio / OpenRouter / OpenAI-compatible providers | outbound HTTPS, allowlist-gated | provider API keys from `.env` |
| Egress: `codex_ro.py` → `codex` CLI → OpenAI | outbound, via the Codex CLI's own auth | `~/.codex/auth.json` |

Evidence for "nothing inbound": no `ports:`, no `EXPOSE`, no `Dockerfile`/compose, no
reverse-proxy or tunnel config, no `socket`/`bind`/`listen` in any Python file, no
`.github/workflows`. Searched across the whole tree except `legacy/`.

## Step 2 — tooling (machines first)

The repo has **no validation entry point of its own** — no Makefile, no CI workflow, no
`pyproject.toml`. Commands were therefore chosen here, not taken from the repo.

| Tool | Version | Result |
|---|---|---|
| gitleaks | 8.30.1 | `gitleaks dir . --no-banner --redact` → 644.62 KB scanned, **no leaks** |
| gitleaks | 8.30.1 | `gitleaks git . --no-banner --redact` → 45 commits, **no leaks** |
| grype | 0.118.0 | `grype dir:.` → **no vulnerabilities** (no dependency manifest exists at all) |
| pytest | 9.0.3 | `python -m pytest -q` → **90 passed, 10 subtests passed**, 3.46 s |
| py_compile | 3.12 | all `.py` compile |
| hadolint | 2.15.1 | **n/a** — no Dockerfile |
| actionlint | 1.7.12 | **n/a** — no `.github/workflows` |
| shellcheck | — | **NOT INSTALLED** → `hooks/claudex-python.sh` was never statically checked |
| ruff, bandit, pip-audit, trufflehog, checkov, interrogate, semgrep | — | **NOT INSTALLED** — did not run |

Scanner output is evidence, not verdict: a clean gitleaks run over a repo whose own
`.gitignore` excludes `.env` proves the ignore works, not that the design is sound.

## Step 3 — reviewer sessions

### Session start failure (recorded, per the "invalid output lands in the log too" rule)

The first four slice calls all exited 1 with an empty answer file. stderr:

```
Error loading config.toml: invalid transport
in `mcp_servers.MCP_DOCKER`
```

Cause, verified: `DEFAULT_DISABLE_MCP = ("n8n", "MCP_DOCKER")` (`scripts/codex_ro.py:64`)
makes the wrapper emit `-c mcp_servers.MCP_DOCKER.enabled=false`. `MCP_DOCKER` is not
configured on this machine, so that override *synthesises* an MCP server table with no
`transport`, and Codex then refuses to load its config at all. Control: the same call
with `--disable-mcp n8n` returned `PROBE OK`. This contradicts the comment on
`scripts/codex_ro.py:61` ("Naming a server that does not exist is harmless") and is
carried into the baseline as a finding. All sessions below were re-run with
`--disable-mcp n8n`.

<!-- reviewer reports appended below as they arrive -->

---

## S1 — wrapper — gpt-5.6-terra/high (verbatim)

## SECURITY

Examined `scripts/codex_ro.py`’s argument, environment, path, subprocess, MCP, and LLM-egress boundaries; and `scripts/wrapper_drift.py`’s update path. These supplied units expose no inbound routes, pages, HTTP endpoints, webhooks, or callback handlers, so authentication/object authorization and bind-address checks are not applicable.

1. **CRITICAL** — The allowlisted caller can turn `--out-file` into an arbitrary write/delete primitive by supplying `--allow-path` or `CLAUDEX_ALLOWED_PATHS`; those values are accepted as roots, after which an existing output is unlinked and output/error streams are opened for writing. `scripts/codex_ro.py:124`, `scripts/codex_ro.py:364`, `scripts/codex_ro.py:429`, `scripts/codex_ro.py:448`.  
   Fix: remove caller-controlled extra roots from the unattended wrapper; use only fixed, administrator-configured output directories.

2. **HIGH** — `CLAUDEX_CODEX_BIN` accepts any existing file, and PATH lookup accepts whichever `codex` shim resolves first. A caller able to influence the environment/PATH can make the permission-allowlisted wrapper execute arbitrary code outside Codex’s read-only sandbox. `scripts/codex_ro.py:218`, `scripts/codex_ro.py:221`, `scripts/codex_ro.py:228`, `scripts/codex_ro.py:450`.  
   Fix: remove the environment override for unattended use and resolve Codex only from a trusted installation path (optionally with ownership/signature checks).

3. **HIGH** — The wrapper’s MCP disablement is bypassable: it appends arbitrary `--config` values after its disabling overrides, and also permits an empty `--disable-mcp` value. This can re-enable configured MCP servers, creating filesystem/network side channels outside the claimed sandbox boundary. `scripts/codex_ro.py:59`, `scripts/codex_ro.py:76`, `scripts/codex_ro.py:185`, `scripts/codex_ro.py:187`, `scripts/codex_ro.py:370`, `scripts/codex_ro.py:384`.  
   Fix: reject `mcp_servers` and provider/egress-related config overrides, and launch with a known MCP-free configuration.

4. **HIGH** — Path confinement is checked before later filesystem mutations, leaving a TOCTOU symlink/reparse-point race: a writable path can be replaced after `realpath()` validation but before `unlink()` or `open()`. `scripts/codex_ro.py:146`, `scripts/codex_ro.py:427`, `scripts/codex_ro.py:429`, `scripts/codex_ro.py:448`.  
   Fix: create outputs in a private trusted directory and use no-follow, descriptor-based file creation/atomic replacement.

5. **HIGH** — `wrapper_drift --update` follows destination directory/file symlinks and overwrites them with `copyfile`; a scanned repository can redirect an update into an unrelated user file. `scripts/wrapper_drift.py:131`, `scripts/wrapper_drift.py:155`, `scripts/wrapper_drift.py:156`.  
   Fix: reject symlinked `tools/` directories and destination files, verify resolved destinations remain below the repository, then use atomic no-follow writes.

No secret literal or secret-bearing response/URL is present in the supplied units. No dependency finding is raised from the supplied scanner output.

## QUALITY

6. **MEDIUM** — `read_thread_id()` loads the entire unbounded JSON event stream merely to find one early event; a large Codex response can cause unnecessary memory exhaustion after an otherwise completed review. `scripts/codex_ro.py:317`, `scripts/codex_ro.py:321`, `scripts/codex_ro.py:326`.  
   Fix: read incrementally and stop after the first `thread.started` event, with a bounded fallback scan.

7. **MEDIUM** — Prompt and output filesystem failures escape as raw exceptions rather than the wrapper’s documented refusal/error protocol; unreadable UTF-8 prompt files, denied directories, and failed truncations produce tracebacks. `scripts/codex_ro.py:419`, `scripts/codex_ro.py:427`, `scripts/codex_ro.py:429`, `scripts/codex_ro.py:448`.  
   Fix: catch `OSError` and `UnicodeError` around filesystem operations and emit a defined diagnostic/exit code.

## DOCS

8. **MEDIUM** — The `--scan` example promises “every repo under ROOT,” but implementation and argument help inspect only one or two directory levels, silently missing deeper stale wrappers. The documentation claim is wrong. `scripts/wrapper_drift.py:25`, `scripts/wrapper_drift.py:89`, `scripts/wrapper_drift.py:92`, `scripts/wrapper_drift.py:108`.  
   Fix: recurse through all descendants or change the public documentation to explicitly state the depth limit.

9. **LOW** — The module documentation says `--update` rewrites drifted copies generally, while code deliberately leaves optional drifted tools unchanged unless `--update-optional` is supplied. The `--update` wording is inaccurate. `scripts/wrapper_drift.py:29`, `scripts/wrapper_drift.py:152`, `scripts/wrapper_drift.py:159`.  
   Fix: state that `--update` affects required tools only.

## TESTS

10. **HIGH** — The read-only guarantee is based on a one-time CLI-version measurement, while the tests explicitly never execute Codex. A future Codex CLI change to `-s`/`-c` precedence could silently reopen the sandbox. `scripts/codex_ro.py:8`, `scripts/codex_ro.py:18`, `tests/test_codex_ro.py:8`, `tests/test_codex_ro.py:10`.  
   Fix: add a version-pinned integration contract test in an isolated disposable repository that attempts writes for both fresh and resumed sessions.

11. **HIGH** — The supplied tests test the path opt-in as successful behavior but do not test that unattended invocations cannot broaden roots, replace the Codex executable, or re-enable MCP through trailing config. `tests/test_codex_ro.py:123`, `tests/test_codex_ro.py:125`, `tests/test_codex_ro.py:76`, `tests/test_codex_ro.py:81`.  
   Fix: add security-policy tests that require these inputs to be refused in the allowlisted mode.

## RULES

No rule-violation finding can be quoted responsibly: `README.md`, `ROLES.md`, `FALLBACK.md`, and `docs/betrieb.md` are referenced but their contents and numbered lines were not included in this audit slice.

not examined: the referenced rule documents, `hooks/wrapper_guard.py`, other scripts/skills, installed target-repository copies, and tests beyond the supplied module.

AUDIT: CRITICAL
---

## S2 — roles — gpt-5.6-terra/high (verbatim)

## SECURITY

Examined configuration discovery/loading, role gates, actor-spec output, CLI paths, example configuration, and role doctrine. This slice has no routes, endpoints, listeners, ports, subprocess calls, HTTP/LLM egress, webhooks, or authentication surface.

1. **HIGH** — A repository-controlled config can disable both security gates and redefine Codex’s actor sandbox. `find_config()` accepts the first `.claudex.yaml` from the current directory or any parent (`scripts/claudex_roles.py:170`, `scripts/claudex_roles.py:172`, `scripts/claudex_roles.py:173`), merges it into defaults (`scripts/claudex_roles.py:190`), and runs the gates only when the config leaves their flags true (`scripts/claudex_roles.py:248`, `scripts/claudex_roles.py:273`). A planted config can set both flags false and set `actors.codex.sandbox` to an open value; the resolver then reports success and emits that sandbox in `--spec`.  
   Fix: Treat gate state and adversary sandbox policy as non-configurable, or accept policy config only from a trusted, owner-controlled location.

2. **HIGH** — The `cross` arrangement accepts one, zero, or duplicate authors, so it can claim an independent review where none exists. Any list is accepted as a producer assignment (`scripts/claudex_roles.py:213`, `scripts/claudex_roles.py:214`); when the reviewer is `cross`, the gate does not require two distinct actors (`scripts/claudex_roles.py:252`, `scripts/claudex_roles.py:256`), and resolves the “reviewers” directly to that same list (`scripts/claudex_roles.py:303`, `scripts/claudex_roles.py:306`, `scripts/claudex_roles.py:308`). Thus `[claude]` or `[claude, claude]` passes.  
   Fix: Require exactly two distinct known actors for a cross-produced artefact and explicitly map each author to the other reviewer.

3. **MEDIUM** — Arbitrary actor configuration fields, including accidentally stored API tokens, are emitted verbatim by both output modes. `actor_spec()` copies every actor property except `roles` (`scripts/claudex_roles.py:321`), `--spec` prints each property (`scripts/claudex_roles.py:368`), and `--json` serializes all actor config (`scripts/claudex_roles.py:373`, `scripts/claudex_roles.py:375`). This also permits malformed values to alter the token stream consumed by wrapper-building automation.  
   Fix: Validate an allowlisted actor schema and never emit secret-bearing fields; use structured output for machine consumers.

## QUALITY

4. **MEDIUM** — Malformed YAML, unreadable config files, and wrong top-level types can escape as Python tracebacks rather than controlled resolver failures. YAML parsing and file reads are outside the `ConfigError` contract (`scripts/claudex_roles.py:155`, `scripts/claudex_roles.py:160`), while `main()` catches only `ConfigError` (`scripts/claudex_roles.py:338`, `scripts/claudex_roles.py:340`). For example, `actors: []` reaches `.items()` (`scripts/claudex_roles.py:222`).  
   Fix: Validate top-level types before use and normalize I/O/YAML parser exceptions to `ConfigError`.

5. **LOW** — The fallback parser corrupts quoted values containing ` #`, despite supporting quoted scalars. It strips comments before scalar parsing (`scripts/claudex_roles.py:128`), whereas quote handling occurs later (`scripts/claudex_roles.py:105`). Behaviour also changes depending on whether PyYAML is installed (`scripts/claudex_roles.py:157`, `scripts/claudex_roles.py:159`).  
   Fix: Make one parser mandatory, or make the minimal parser quote-aware and enforce its documented grammar.

## DOCS

6. **MEDIUM** — The code contradicts the documented repository-root config scope. ROLES says “`.claudex.yaml` in the repo root” (`ROLES.md:48`), but the resolver accepts a config from any nested working-directory ancestor before reaching `.git` (`scripts/claudex_roles.py:170`, `scripts/claudex_roles.py:172`, `scripts/claudex_roles.py:175`). The code is wrong: a nested directory must not override the repository’s policy file.  
   Fix: Locate the repository root first, then consider only its `.claudex.yaml` before the user-level config.

## TESTS

7. **MEDIUM** — No test rejects invalid cross arrangements such as `[claude]`, `[]`, or `[claude, claude]`. The delegation test covers only scalar producer/reviewer identity (`tests/test_roles.py:62`, `tests/test_roles.py:65`, `tests/test_roles.py:69`), leaving the independent-review bypass untested.  
   Fix: Add parameterized rejection tests for zero, one, and duplicate cross authors.

8. **MEDIUM** — Error-path tests do not exercise malformed YAML, unreadable config, or invalid top-level containers through the public CLI. Existing validation tests cover only per-role override content (`tests/test_roles.py:52`, `tests/test_roles.py:59`), and the CLI test uses defaults (`tests/test_roles.py:72`, `tests/test_roles.py:77`).  
   Fix: Invoke `main()` against temporary invalid configs and assert exit code 2 with a concise diagnostic and no traceback.

9. **LOW** — The exposure-pair test is coupled to private constants rather than observable resolver behaviour (`tests/test_roles.py:32`, `tests/test_roles.py:34`, `tests/test_roles.py:35`). A refactor can preserve the contract while failing this test, or preserve these internals while breaking CLI resolution.  
   Fix: Test `--role`/`--spec` and gate outcomes using config fixtures instead.

## RULES

10. **HIGH** — The opt-out flags violate the stated mandatory adversary rule: “**Adversary** … must be **read-only** and a **foreign context**” (`ROLES.md:27`, `ROLES.md:28`), and “**Every other adversary rule still applies**” (`ROLES.md:135`). Setting `adversary_read_only: false` skips the only enforcement loop (`scripts/claudex_roles.py:273`), including its Codex sandbox check (`scripts/claudex_roles.py:277`).  
   Fix: Remove the disabling switch for adversary roles and fail closed regardless of project config.

Coverage note: not examined: `codex_ro.py`, `fallback_review.py`, hook enforcement, skills, README/FALLBACK/betrieb documentation, and runtime integrations that consume `--spec`.

AUDIT: CONCERNS
---

## S3 — fallback — gpt-5.6-terra/high (verbatim)

## SECURITY

No inbound routes, pages, webhooks, callbacks, bind addresses, or ports exist in this slice; authentication/authorisation are therefore N/A. I examined the outgoing `/models`, `/credits`, and `/chat/completions` requests, `.env` configuration, dynamic PyYAML import, JSON/YAML parsing, and `lms` subprocess calls. No inline secrets or shell-string invocation were found.

1. **CRITICAL** — A target repository can supply the working-directory `.env`, configure an arbitrary HTTPS reviewer, and receive the plan and review log. `scripts/fallback_review.py:100` (`100| def load_dotenv(path=".env"):`) and `scripts/fallback_review.py:666` (`666|     load_dotenv()`) load that file automatically; with no allowlist, `scripts/fallback_review.py:353` (`353|                     "the variable to drop the host restriction.")`) deliberately permits any HTTPS host, and `scripts/fallback_review.py:523` (`523|     try:`) opens the plan-carrying request. In this plugin’s approval-bypass threat model, a malicious checkout can exfiltrate review material without a new prompt.  
   Fix: only load an explicitly selected, trusted configuration file outside the target checkout and default-deny remote egress unless a trusted allowlist approves it.

2. **HIGH** — The egress guard checks only the initial URL; `urllib` can follow a server-supplied redirect without re-running `check_egress`. This defeats the exact-host policy and can forward an API credential to a redirected host: `scripts/fallback_review.py:370` (`370|     check_egress(url)  # every request, not just the one the profile declared`) is followed directly by `scripts/fallback_review.py:372` (`372|     with urllib.request.urlopen(req, timeout=timeout) as resp:`), with the same pattern for the review request at `scripts/fallback_review.py:520` (`520|     check_egress(chat_url)  # the request that actually carries the plan`) and `scripts/fallback_review.py:523` (`523|     try:`).  
   Fix: disable redirects, or implement a redirect handler that revalidates every `Location` and strips credentials on an origin change.

3. **MEDIUM** — The file allowlist security control dynamically imports PyYAML, but the repository has no dependency manifest for the supplied scanner to inventory or pin: `scripts/fallback_review.py:254` (`254|         import yaml`). Missing PyYAML fails closed, but an installed vulnerable or unexpected version is outside dependency review.  
   Fix: declare and pin PyYAML with a supported minimum version, or remove the optional dependency from the enforcement path.

## QUALITY

1. **MEDIUM** — Preflight treats server failures as availability. Only 401/403 and 402/429 return failure; a 500, 503, or malformed HTTP status falls through the “reachable is good enough” branch at `scripts/fallback_review.py:446` (`446|         # Other codes (404/405 …): some compat layers don't expose /models —`) and eventually returns success at `scripts/fallback_review.py:464` (`464|     return True, "reachable + auth ok (no balance API — exhaustion only shows on use)"`). This burns a review attempt against a predictably unhealthy provider.  
   Fix: accept successful responses, explicitly tolerate only documented compatibility statuses, and mark other 4xx/5xx responses unavailable.

2. **HIGH** — `lms` subprocesses have no timeout, so a hung local runtime blocks preflight and the whole loop indefinitely despite a configured reviewer timeout: `scripts/fallback_review.py:404` (`404|             subprocess.run(["lms", "unload", model], capture_output=True)`) and `scripts/fallback_review.py:411` (`411|     result = subprocess.run(`).  
   Fix: set a bounded subprocess timeout and convert `TimeoutExpired`/`OSError` into a reported unavailable result.

3. **MEDIUM** — The anti-rubber-stamp gate counts any numbered line, not findings. `scripts/fallback_review.py:491` (`491|     return len(re.findall(r"^\s*\d{1,2}[.)]\s+\S", text, flags=re.M))`) lets a three-item summary or copied outline satisfy the minimum; the claim that over-counting is harmless at `scripts/fallback_review.py:489` (`489|     only feeds the rubber-stamp lower bound, so over-counting is harmless.`) is false.  
   Fix: require a structured finding format with location, impact, and fix before counting it.

4. **MEDIUM** — A failed request leaves an existing `--out` verdict untouched, contradicting the claim that readers ignoring an exit code cannot mistake invalid output for a recorded round. The output is written only after a successful response/validation at `scripts/fallback_review.py:560` (`560|     if args.out:`), while transport errors escape at `scripts/fallback_review.py:529` (`529|         raise ProviderError(f"HTTP {exc.code} from {p['base_url']} — {detail}",`). A stale approval for the same plan can therefore remain at the requested path after a failed invocation.  
   Fix: write to a fresh temporary file and atomically replace the target only on success, or mark/remove the requested target before attempting the call.

5. **MEDIUM** — Quota detection examines only the ten newest rollout files: `scripts/codex_usage.py:34` (`34|     for path in files[:10]:`). Ten newer rollouts without `rate_limits` cause a false “no snapshot” despite an older valid snapshot, defeating the “most recent rate_limits entry” contract at `scripts/codex_usage.py:31` (`31|     """Most recent rate_limits entry across rollout files (newest files first)."""`).  
   Fix: scan until a snapshot is found, rather than applying a file-count cutoff.

## DOCS

1. **MEDIUM** — The documented verdict contract says the verdict must terminate the reply: `scripts/fallback_review.py:94` (`94|     "your output budget is for findings. End your reply with EXACTLY one line: "`). The implementation instead accepts the last matching verdict anywhere in the output: `scripts/fallback_review.py:591` (`591|         return found[-1].upper() if found else None`). Text after `VERDICT: APPROVED` is silently accepted. The code is wrong if the documented end-of-reply grammar is intended as a validity boundary.  
   Fix: trim trailing whitespace and validate that the final nonblank line is the required verdict.

## TESTS

1. **HIGH** — The supplied egress test proves only that the original URL is checked before opening: `tests/test_egress.py:203` (`203|     def test_the_get_helper_checks_before_opening_the_connection:`) through `tests/test_egress.py:212` (`212|         self.assertEqual(opened, [], "nothing may be sent before the check passes")`). It does not test a 30x redirect to an unallowlisted host, which is the bypass in Security finding 2.  
   Fix: use a local redirecting HTTP server/opener test and assert that redirects are rejected or separately egress-validated.

2. **MEDIUM** — The allowlist tests are not hermetic: `tests/test_egress.py:52` (`52|     def setUp(self):`) saves only `CLAUDEX_EGRESS_ALLOW`, while `CLAUDEX_EGRESS_ALLOWLIST` remains live. A developer/CI environment with a file allowlist can change the result of unrelated host-list tests.  
   Fix: isolate both egress environment variables and patch `_allowlist_source()` in every egress test fixture.

## RULES

1. **HIGH** — The mandatory logging rule is violated. FALLBACK.md states, verbatim, **“Every fallback round is recorded as:”** at `FALLBACK.md:140` (`140| Every fallback round is recorded as:`) and **“Findings never live only in the chat.”** at `FALLBACK.md:152` (`152|     entry, as for any Codex round. Findings never live only in the chat.`). Yet `--append-log` is optional at `scripts/fallback_review.py:638` (`638|     ap.add_argument("--append-log",`) and a valid review simply prints to stdout when absent at `scripts/fallback_review.py:564` (`564|     else:`).  
   Fix: require `--append-log` for review execution, or create a mandatory default log entry.

2. **MEDIUM** — The written log heading does not meet the documented mandatory label. FALLBACK.md requires, verbatim, **“## Round <n> — <model> (via <reviewer>, fallback — plan-text only, no repo access)”** at `FALLBACK.md:143` (`143| ## Round <n> — <model> (via <reviewer>, fallback — plan-text only, no repo access)`). The implementation writes only `fallback` at `scripts/fallback_review.py:570` (`570|         entry = (f"\n## Round {args.round_no} — {p['model']} (via {p['name']}, {label})\n\n"`), omitting the limitation the rule requires readers to see.  
   Fix: emit the exact required phrase in every fallback log header.

Coverage note: not examined: files outside `S3-fallback`, the wrapper hook and skills that invoke these commands, and the full text of README.md, ROLES.md, and docs/betrieb.md, which were referenced but not supplied inline.

AUDIT: CRITICAL
---

## S4 — hooks — gpt-5.6-terra/high (verbatim)

## SECURITY

Checked `wrapper_guard.py`, `claudex-python.sh`, and `hooks.json`. This slice exposes no routes, endpoints, listeners, ports, authentication, secrets, or network egress. Its security boundary is the allowlisted shell invocation.

1. **CRITICAL** — `hooks/wrapper_guard.py:155`  
   `  155|     if not any(_invokes_wrapper(segment) for segment in segments):`  
   Bash can expand the wrapper filename after this check: `python tools/codex_ro.py${IFS}&&whoami` matches the allowlist prefix and executes the wrapper plus `whoami`, but the token is not recognized as ending in `codex_ro.py`, so the guard allows it.  
   Fix: reject shell expansion syntax in the command/script token and add expansion-aware parsing or a conservative raw-token policy.

2. **HIGH** — `hooks/wrapper_guard.py:177`  
   `  177|         return 0  # not our business; never block on unparseable input`  
   A malformed or schema-incompatible PreToolUse payload disables the guard with a successful exit code, leaving an allowlisted wrapper invocation unprotected.  
   Fix: fail closed when malformed input plausibly concerns `codex_ro`, while continuing to pass unrelated malformed events.

Dependency risk: no dependency-handling code is present in this hook slice beyond the supplied scanner results.

## QUALITY

3. **LOW** — `hooks/wrapper_guard.py:162`  
   `  162|     for pattern in SUBSTITUTION_PATTERNS:`  
   The raw-string scan treats `$(` and backticks inside quoted arguments as executable substitution, unlike the correct handling of quoted semicolons. Valid literal prompt or filename text can therefore be denied.  
   Fix: retain quote context while tokenising and only reject substitution syntax that is active in the relevant shell grammar.

## DOCS

4. **LOW** — `hooks/wrapper_guard.py:35`  
   `   35| Anything it cannot parse is passed through: a guard that crashes closed on`  
   This contradicts the command-parser behavior: unbalanced quotes involving the wrapper are denied, not passed through. The text is wrong; the code’s safer behavior should remain.  
   Fix: document the distinction between malformed hook payloads (allowed) and unparseable wrapper commands (denied).

## TESTS

5. **MEDIUM** — `tests/test_wrapper_guard.py:111`  
   `  111|     def test_and_chaining_is_denied(self):`  
   Tests cover literal chaining operators but not the `${IFS}` wrapper-token expansion that bypasses invocation detection, so the critical boundary failure has no regression test. The suite also invokes the Python guard directly rather than validating the configured hook/shim path.  
   Fix: add Bash execution/guard cases for expansion-obscured wrapper paths and an integration test for `hooks.json` plus `claudex-python.sh`.

## RULES

No rule violation can be substantiated from the supplied slice: the contents of `README.md`, `ROLES.md`, `FALLBACK.md`, and `docs/betrieb.md` were not included.

Coverage note: not examined: `scripts/codex_ro.py`, `scripts/claudex_roles.py`, `scripts/fallback_review.py`, installed permission configuration, and the contents of the referenced rule documents.

AUDIT: CRITICAL
---

## S5a — skills core — gpt-5.6-terra/high (verbatim)

## SECURITY

Checked the review/build subprocess contracts, remote-CodeX egress, and exposure-pass commands. This documentation slice defines no inbound routes, pages, ports, or authentication surface.

1. **HIGH — `skills/claudex-loop/SKILL.md:230`** reads a relative, repo-controlled `REVIEW_PROMPT` file that the workflow never creates. A malicious target repository can supply it and steer the remote reviewer to read and disclose sensitive workspace material; on an ordinary repo the review instead fails before receiving the documented prompt.  
   Fix: write the fixed prompt into a controlled scratch file and feed it via stdin, never read a prompt file from the target repo.

2. **HIGH — `skills/code-review/SKILL.md:264`; `skills/audit/SKILL.md:230`** invoke `tools/codex_ro.py`, while the repository context identifies the mandatory read-only wrapper as `scripts/codex_ro.py`. The exposure pass therefore fails to start, leaving network-facing target changes/components without the required security review.  
   Fix: correct both invocations to `python scripts/codex_ro.py` and fail the gate when no exposure verdict is produced.

3. **MEDIUM — `skills/claudex-loop/SKILL.md:224`** authorizes the remote reviewer to read “any repo files you need” without an egress consent, secret-exclusion rule, or bounded file list. Read-only protects local files from mutation, not source-code disclosure to the LLM service.  
   Fix: require an explicit egress acknowledgement and restrict review inputs to an allowlisted, secret-screened set of files.

## QUALITY

4. **HIGH — `skills/code-review/SKILL.md:367`** hard-codes verdict requirements for all five scopes, although the default scope excludes `docs` and `tests` at `skills/code-review/SKILL.md:46`. During a Codex outage, a valid fallback response for the requested scopes is rejected, defeating the documented fallback path.  
   Fix: generate `--require-verdicts` from the selected `scope` value.

5. **MEDIUM — `skills/claudex-loop/SKILL.md:250`–`skills/claudex-loop/SKILL.md:254`** defines actions only for `APPROVED` and `REVISE`; a non-empty output with a missing or malformed final verdict is logged but has no failure path. The review can silently stop making progress instead of being marked invalid.  
   Fix: require exactly one valid final verdict and stop/log an invalid attempt otherwise.

6. **MEDIUM — `skills/build/SKILL.md:64`; `skills/build/SKILL.md:110`; `skills/build/SKILL.md:136`** requires per-round report names but never shows initialization or increment of `ROUND`; following the snippets can reuse `r0` and overwrite the initial build report during a fix round.  
   Fix: show explicit round initialization/increment and assert that each report path is new before invoking Codex.

## DOCS

7. **MEDIUM — `skills/build/SKILL.md:76`; `skills/build/SKILL.md:92`** says `mktemp` does not land in `/tmp`, but standard `mktemp` uses `TMPDIR` or `/tmp` by default. The prose is wrong and contradicts the stated scratch-file handling.  
   Fix: create the prompt with a template under `SCRATCH_DIR` (or document and enforce a safe `TMPDIR` exception).

## TESTS

8. **HIGH — `skills/claudex-loop/SKILL.md:172`; `skills/claudex-loop/SKILL.md:230`** claims the launch mechanics were verified end-to-end, yet the current documented Round-1 command depends on the undeclared `REVIEW_PROMPT` file. The passing suite therefore does not exercise this exact clean-repository launch path.  
   Fix: add an integration smoke test that runs the documented command with no pre-existing prompt file and asserts a captured thread plus valid verdict.

9. **HIGH — `skills/code-review/SKILL.md:46`; `skills/code-review/SKILL.md:367`** lacks a regression test for fallback review with a subset scope; the hard-coded all-scope verdict contract remains despite contradicting the default scope.  
   Fix: parameterize fallback-contract tests across every supported scope combination.

## RULES

10. **HIGH — `skills/code-review/SKILL.md:264`; `skills/code-review/SKILL.md:402`** violates the rule, “**The exposure pass is not a scope and cannot be deselected.**” The nonexistent wrapper path makes the mandatory pass operationally absent.  
    Fix: use the repository’s `scripts/codex_ro.py` path and treat execution failure as an unreviewed, failed gate.

11. **MEDIUM — `skills/build/SKILL.md:72`; `skills/build/SKILL.md:92`** violates the rule, “**⛔ Never `/tmp`.**” The documented bare `mktemp` command normally creates its file there.  
    Fix: use `mktemp "$SCRATCH_DIR/prompt.XXXXXX"` or remove the absolute rule and define a secured exception.

Coverage note: examined the four supplied core skill documents and their subprocess, sandbox, egress, fallback, and exposure-review instructions. Not examined: implementations of `scripts/`, `hooks/`, tests, and the contents of README.md, ROLES.md, FALLBACK.md, and docs/betrieb.md beyond the supplied context.

AUDIT: CONCERNS
---

## S5b — skills rest + docs — gpt-5.6-terra/high (verbatim)

## SECURITY

Examined `plan-review`, `docs-backfill`, `setup`, and the hook manifest. This slice defines no inbound routes, pages, endpoints, listeners, `ports:`, signup, credentials, webhook handlers, or callback handlers. Its material surface is Codex subprocess execution and source-code egress.

1. **HIGH** — `docs-backfill` permits any `TARGET` and gives the remote reviewer access to code without first resolving or enforcing a taboo/egress scope. A target repo containing customer or credential-adjacent source can therefore be exposed to Codex/OpenAI despite the plugin’s stated threat model. (`skills/docs-backfill/SKILL.md:  42|`, `skills/docs-backfill/SKILL.md:  110|`, `skills/docs-backfill/SKILL.md:  124|`)  
   Fix: require a fail-closed approved-path/denied-path preflight and review from an isolated approved-content tree, not merely a reviewer instruction.

2. **HIGH** — Safety-critical calls bypass `tools/codex_ro.py`: plan review invokes `codex exec` and `codex exec resume` directly, and setup’s verification ping does too. This bypasses the canonical enforcement point for path bounds, per-server MCP shutdown, normalized stderr handling, and timeout behavior; a configured MCP can consequently receive repository context outside the intended wrapper controls. (`skills/plan-review/SKILL.md:  132|`, `skills/plan-review/SKILL.md:  150|`, `skills/setup/SKILL.md:  283|`)  
   Fix: make every Codex invocation, including ping and resume, call the wrapper exclusively.

Dependency risk: this slice has no dependency manifest or lockfile to review; the supplied scanner baseline contains no resolved dependency finding.

## QUALITY

3. **HIGH** — `PLAN_FILE` is advertised as configurable, but both reviewer prompts hard-code `PLAN.md`. With a non-default plan path, Codex may review a stale file or no plan at all while the user signs off a different one. (`skills/plan-review/SKILL.md:  55|`, `skills/plan-review/SKILL.md:  127|`, `skills/plan-review/SKILL.md:  152|`)  
   Fix: inline the exact resolved plan content in every prompt and include its resolved path only as context.

4. **HIGH** — An empty verdict is initially treated as successful if the file and thread exist, although the documented auth/quota failure mode is exactly “exit 0 plus empty verdict”; the protocol waits for two empty results before treating it as terminal. The first failed review therefore has no fail-closed disposition path. (`skills/plan-review/SKILL.md:  139|`, `skills/plan-review/SKILL.md:  165|`)  
   Fix: require a non-empty output ending in a valid verdict for every round; otherwise stop immediately, inspect stderr, and ask the user how to proceed.

## DOCS

5. **HIGH** — `plan-review` instructs Codex to read `PLAN.md` from the repository, while the verified operating documentation says a fresh/untracked plan is not reliably readable and must be sent inline. The skill text is wrong and can produce an approval of code context rather than the plan. (`skills/plan-review/SKILL.md:  127|`, `docs/betrieb.md:  168|`)  
   Fix: replace the file-reading instruction with an inline, hash-bound plan payload.

6. **MEDIUM** — Model guidance has drifted: `plan-review` says never pin a model, while `setup` and the operating notes say the verified configuration is a `gpt-5.6-terra`/high pin and explicitly warn that `sol` can time out. Following the former can make normal reviews exceed their deadline. (`skills/plan-review/SKILL.md:  46|`, `skills/setup/SKILL.md:  38|`, `docs/betrieb.md:  139|`)  
   Fix: centralize the resolved model policy in the wrapper/role resolver and remove obsolete no-pin guidance.

7. **HIGH** — The advertised manual installation copies only `skills/*`, omitting scripts and the hook that protects an allowlisted wrapper call; the README acknowledges the missing scripts but does not make the missing hook and plugin lifecycle explicit. The documented manual path therefore cannot safely provide the claimed setup. (`README.md:  145|`, `README.md:  149|`, `README.md:  157|`, `README.md:  214|`)  
   Fix: either remove manual installation as a supported secure path or provide explicit installation/activation steps for scripts and `hooks/`.

## TESTS

8. **HIGH** — The passing test suite does not guard the safety contract encoded in these skill recipes: it allowed direct Codex invocations to remain in `plan-review` and `setup`. A regression test should fail whenever a skill’s executable recipe invokes `codex exec` instead of the wrapper, including resume and ping paths. (`skills/plan-review/SKILL.md:  132|`, `skills/plan-review/SKILL.md:  150|`, `skills/setup/SKILL.md:  283|`)  
   Fix: add a repository-level contract test that parses skill command blocks and permits direct Codex calls only where explicitly justified.

## RULES

Finding 2 violates the documented rule: “**Auch Ping und Resume laufen über den Wrapper.**” (`docs/betrieb.md:  51|`). The direct invocations at `skills/plan-review/SKILL.md:  132|`, `skills/plan-review/SKILL.md:  150|`, and `skills/setup/SKILL.md:  283|` break it.  
Fix: route those recipes through `tools/codex_ro.py`.

Finding 5 violates the documented rule: “**Codex sieht `PLAN.md` nicht zuverlässig — Plantext inline in den Prompt.**” (`docs/betrieb.md:  168|`). The reviewer prompt at `skills/plan-review/SKILL.md:  127|` still relies on the file.  
Fix: inline the resolved plan text for every review round.

not examined: `scripts/`, hook implementation bodies, fallback implementation, role resolver, tests themselves, legacy skills, and any runtime behavior outside this S5b documentation/manifest slice.

AUDIT: CONCERNS
---

## S6 — legacy — gpt-5.6-terra/high (verbatim)

## SECURITY

Examined both legacy skill workflows and their companion formats/licenses. There are no inbound routes, pages, endpoints, bind addresses, webhook handlers, or credentials in this slice; authentication/authorisation are therefore not applicable. The documented commands quote `THREAD_ID`; no direct shell-injection or traversal input is shown. The supplied dependency scan gives no additional dependency finding.

1. **CRITICAL** — Both workflows write reviewer output to the predictable shared path `/tmp/codex-verdict.txt`. A local process can pre-create it as a symlink, causing the CLI’s output write to overwrite an arbitrary victim-writable file; concurrent reviews can also consume or overwrite one another’s verdicts.

   `legacy/grill-me-codex/SKILL.md:    79| codex exec -s read-only --json -o /tmp/codex-verdict.txt "$(cat REVIEW_PROMPT)" \`

   `legacy/grill-me-codex/SKILL.md:    90|   -o /tmp/codex-verdict.txt \`

   `legacy/grill-with-docs-codex/SKILL.md:   159| codex exec -s read-only --json -o /tmp/codex-verdict.txt "$(cat REVIEW_PROMPT)" \`

   `legacy/grill-with-docs-codex/SKILL.md:   170|   -o /tmp/codex-verdict.txt \`

   Fix: create a private per-run temporary directory/file securely, reject symlinks, and clean it up.

2. **HIGH** — The legacy skills invoke `codex` directly instead of the plugin’s read-only wrapper, so they bypass the product’s enforcement point that is meant to make opening the sandbox impossible. The inline flags are advisory configuration of a raw CLI invocation, not wrapper-level policy enforcement.

   `legacy/grill-me-codex/SKILL.md:    79| codex exec -s read-only --json -o /tmp/codex-verdict.txt "$(cat REVIEW_PROMPT)" \`

   `legacy/grill-me-codex/SKILL.md:    89| codex exec resume "$THREAD_ID" -c sandbox_mode="read-only" --json \`

   `legacy/grill-with-docs-codex/SKILL.md:   159| codex exec -s read-only --json -o /tmp/codex-verdict.txt "$(cat REVIEW_PROMPT)" \`

   `legacy/grill-with-docs-codex/SKILL.md:   169| codex exec resume "$THREAD_ID" -c sandbox_mode="read-only" --json \`

   Fix: replace these examples with the guarded `codex_ro.py` invocation and make the wrapper own sandbox selection.

3. **HIGH** — Resume failures are deliberately hidden and the instructions do not delete or validate the prior verdict before reuse. A failed resume can leave an old verdict in place, which is then logged and treated as the current review.

   `legacy/grill-me-codex/SKILL.md:    92|   < /dev/null 2>/dev/null >/dev/null`

   `legacy/grill-me-codex/SKILL.md:    99| 1. Read `/tmp/codex-verdict.txt`; append to `LOG_FILE`: `## Round <n> — Codex` + the full critique.`

   `legacy/grill-with-docs-codex/SKILL.md:   172|   < /dev/null 2>/dev/null >/dev/null`

   `legacy/grill-with-docs-codex/SKILL.md:   179| 1. Read verdict file; append `## Round <n> — Codex` + critique to `LOG_FILE`.`

   Fix: require a zero exit status and a newly created, run-specific verdict file before processing any verdict.

4. **MEDIUM** — The skills permit Codex to read arbitrary repository files without an egress warning, secret-redaction step, or explicit approval that source may be sent to a remote model provider. Read-only access does not prevent source-code disclosure.

   `legacy/grill-me-codex/SKILL.md:    75| > You are an adversarial reviewer for an implementation plan. Be skeptical and specific — your job is to find what breaks, not to be agreeable. Read the plan at `PLAN.md` and any repo files you need (you are read-only). Identify concrete flaws: security holes, race conditions, missing edge cases, schema conflicts, wrong assumptions, observability gaps, simpler alternatives. For each, give a one-line fix. Do NOT modify any files. End your reply with EXACTLY one line: \`VERDICT: APPROVED\` if the plan is sound enough to implement, or \`VERDICT: REVISE\` if it still has material problems.`

   Fix: add an egress disclosure and opt-in/redaction preflight before allowing repository exploration.

## QUALITY

5. **HIGH** — The first-round command reads a `REVIEW_PROMPT` file that the skill never creates. In a normal shell, `cat` fails and the command can invoke Codex with an empty prompt, invalidating the review.

   `legacy/grill-me-codex/SKILL.md:    79| codex exec -s read-only --json -o /tmp/codex-verdict.txt "$(cat REVIEW_PROMPT)" \`

   `legacy/grill-with-docs-codex/SKILL.md:   159| codex exec -s read-only --json -o /tmp/codex-verdict.txt "$(cat REVIEW_PROMPT)" \`

   Fix: pass the documented prompt directly via a quoted heredoc or create and validate the prompt file first.

## DOCS

6. **MEDIUM** — `PLAN_FILE` is documented as configurable, but every reviewer prompt hard-codes `PLAN.md`. With `PLAN_FILE=other.md`, Codex reviews a different (possibly stale) file while Claude revises and presents `other.md`.

   `legacy/grill-me-codex/SKILL.md:    69| | \`PLAN_FILE\` | \`PLAN.md\` | The plan Act 1 produced. |`

   `legacy/grill-me-codex/SKILL.md:    75| > You are an adversarial reviewer for an implementation plan. Be skeptical and specific — your job is to find what breaks, not to be agreeable. Read the plan at \`PLAN.md\` and any repo files you need (you are read-only). Identify concrete flaws: security holes, race conditions, missing edge cases, schema conflicts, wrong assumptions, observability gaps, simpler alternatives. For each, give a one-line fix. Do NOT modify any files. End your reply with EXACTLY one line: \`VERDICT: APPROVED\` if the plan is sound enough to implement, or \`VERDICT: REVISE\` if it still has material problems.`

   `legacy/grill-with-docs-codex/SKILL.md:   149| | \`PLAN_FILE\` | \`PLAN.md\` | The plan from Act 1. |`

   `legacy/grill-with-docs-codex/SKILL.md:   155| > You are an adversarial reviewer for an implementation plan. Be skeptical and specific — your job is to find what breaks, not to be agreeable. Read the plan at \`PLAN.md\` (and \`CONTEXT.md\`/ADRs for the domain language) and any repo files you need (you are read-only). Identify concrete flaws: security holes, race conditions, missing edge cases, schema conflicts, domain-language mismatches, wrong assumptions, observability gaps, simpler alternatives. For each, give a one-line fix. Do NOT modify any files. End with EXACTLY one line: \`VERDICT: APPROVED\` or \`VERDICT: REVISE\`.`

   Fix: interpolate one validated plan path consistently into every prompt and operation.

## TESTS

7. **MEDIUM** — No test material in this slice exercises the documented failure gate: failed or timed-out execution, missing verdict output, stale output, and custom `PLAN_FILE` must not produce an approval. A passing happy-path suite cannot establish this safety property.

   `legacy/grill-me-codex/SKILL.md:    82| Parse \`thread_id\` from the \`{"type":"thread.started","thread_id":"..."}\` line → that's \`THREAD_ID\`. The critique is in \`/tmp/codex-verdict.txt\`. Confirm success by the verdict file + a \`thread.started\` line; if neither appears, the run failed (auth/model) — stop and tell the user. \`2>/dev/null\` suppresses cosmetic MCP/auth stderr noise. **\`< /dev/null\` is mandatory:** \`codex exec\` reads stdin *in addition to* the prompt arg, so under a non-interactive driver (Claude Code's Bash tool, CI, any non-TTY pipeline) it blocks forever waiting on stdin EOF — a silent ~0% CPU hang. The redirect gives it immediate EOF.`

   Fix: add isolated command-wrapper tests covering non-zero exit, timeout, missing/freshness-checked verdicts, concurrent runs, and non-default plan paths.

## RULES

Finding 6 violates the skill’s own promised final gate: the user may be told that the configured final plan survived review even though Codex reviewed only `PLAN.md`.

> “**APPROVED:** present the final `PLAN_FILE` ... *‘Grilled + survived N rounds of Codex.’*”

`legacy/grill-me-codex/SKILL.md:   106| - **APPROVED:** present the final \`PLAN_FILE\`, a 3-bullet summary of what the two acts improved, and the round count. Ask: *"Grilled + survived N rounds of Codex. Implement it now — Codex builds it (\`/build\`), Claude builds it, or stop here?"* Code only on a yes. **No code is written during either act.**`

Coverage note: examined all supplied S6 legacy files. Not examined: the referenced repository-rule files (`README.md`, `ROLES.md`, `FALLBACK.md`, `docs/betrieb.md`) and non-slice implementation/test files, which were not available in the supplied source slice.

AUDIT: CRITICAL
---

## Exposure — egress boundary — gpt-5.6-sol/medium (verbatim)

## 1. Reachability / egress

Examined destination parsing, environment and file allowlists, loopback handling, redirects, DNS behavior, and proxy handling.

1. **CRITICAL — loopback restrictions are bypassable through proxy environment variables.**  
   Anchor: `scripts/fallback_review.py:357` — `if scheme == "http" and host not in LOOPBACK_HOSTS:`  
   Anchor: `scripts/fallback_review.py:523` — `with urllib.request.urlopen(req, timeout=p["timeout"]) as resp:`  
   Attack: `urllib.request.urlopen` honors `HTTP_PROXY`/`HTTPS_PROXY`/`NO_PROXY`. A profile such as `http://127.0.0.1:1234/v1` can therefore send the plan, review log, and possibly credentials in cleartext through an attacker-selected HTTP proxy unless proxy bypass happens to be configured. The hostname check validates the URL, not the actual transport path.  
   Fix: use a proxy-free opener for loopback, reject proxy variables for local profiles, and connect only to a resolved loopback address.

2. **HIGH — the egress “allowlist” defaults open and can be disabled by unsetting one variable.**  
   Anchor: `scripts/fallback_review.py:313` — `Unset means no host restriction; the scheme rule still holds.`  
   Anchor: `scripts/fallback_review.py:352` — `f"({', '.join(sorted(names))}). Add it deliberately, or unset "`  
   Anchor: `scripts/fallback_review.py:353` — `"the variable to drop the host restriction.")`  
   Attack: with neither allowlist configured, any HTTPS hostname is accepted. A prompt-injected builder can delete the repository allowlist, unset `CLAUDEX_EGRESS_ALLOW`, or supply a malicious `.env` profile and exfiltrate repository content to an arbitrary TLS endpoint. The repository-local default allowlist is also writable by the party it is meant to constrain.  
   Fix: require a valid, externally controlled allowlist for every remote request; make unset, empty, missing, and deleted policy fail closed.

3. **HIGH — redirects are not re-authorized and can forward credentials outside the allowlist.**  
   Anchor: `scripts/fallback_review.py:370` — `check_egress(url)  # every request, not just the one the profile declared`  
   Anchor: `scripts/fallback_review.py:372` — `with urllib.request.urlopen(req, timeout=timeout) as resp:`  
   Anchor: `scripts/fallback_review.py:520` — `check_egress(chat_url)  # the request that actually carries the plan`  
   Attack: these checks run before `urlopen`, but its default redirect handler follows redirects internally without invoking `check_egress` for the new URL. GET preflight requests carry `Authorization`; Python’s redirect handling can copy that header to another host. Redirect scheme and destination are consequently outside the policy.  
   Fix: disable automatic redirects or implement a redirect handler that revalidates every hop and strips credentials whenever origin changes.

4. **HIGH — `host.docker.internal` is treated as inherently local although it is only a hostname.**  
   Anchor: `scripts/fallback_review.py:197` — `# Hosts where plain http is harmless because the traffic never leaves the`  
   Anchor: `scripts/fallback_review.py:200` — `LOOPBACK_HOSTS = frozenset({"127.0.0.1", "localhost", "::1", "host.docker.internal"})`  
   Attack: outside the expected Docker Desktop DNS setup, `host.docker.internal` can resolve through ordinary or attacker-controlled DNS. The code then permits cleartext plan and log transmission to the resolved non-loopback address and labels it local. `localhost` is likewise trusted by name without resolution verification.  
   Fix: remove `host.docker.internal` from the loopback set and verify every resolved address is actually loopback before allowing HTTP.

DNS rebinding is otherwise not addressed: policy compares the textual hostname only and never pins or validates resolved addresses. Exact textual matching correctly rejects `127.0.0.2`, `127.x`, `::ffff:127.0.0.1`, and suffix-spoofed names, but does not protect an allowed DNS name whose answers change.

## 2. Authentication on every route

Inbound authentication is n/a because nothing accepts inbound requests. Examined provider-key selection and destination binding.

5. **HIGH — API keys are not bound to a provider or expected hostname.**  
   Anchor: `scripts/fallback_review.py:136` — `base_url = get("BASE_URL")`  
   Anchor: `scripts/fallback_review.py:143` — `key_env = get("API_KEY_ENV")`  
   Anchor: `scripts/fallback_review.py:152` — `api_key = os.environ.get(key_env)`  
   Attack: a malicious or mistaken profile can set its base URL to an attacker host and point `API_KEY_ENV` at `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, or another profile’s secret. The key is then sent during preflight and review. Profile names such as `foo-bar`, `foo.bar`, and `foo_bar` also collapse to the same environment prefix at line 131.  
   Fix: bind each credential reference to an explicit hostname/provider identity, reject normalized profile-name collisions, and refuse credentials at any other origin.

## 3. Authorisation per object

Inbound object authorisation is n/a because there are no inbound objects. For profile isolation, checked and found the same issue as finding 5: any profile can name any environment variable and any permitted `base_url`, so one profile’s credentials can be used against another profile’s endpoint.

## 4. Identity shortcuts and security-relevant environment

Examined all security-relevant variables:

- `CLAUDEX_EGRESS_ALLOW`: unset or empty permits every HTTPS host; garbage becomes a restrictive literal hostname list. Unsetting disables protection — finding 2.
- `CLAUDEX_EGRESS_ALLOWLIST`: nonempty missing/broken/unparseable fails closed; empty disables the explicit file and falls back to the repository file or open HTTPS behavior.
- `CLAUDEX_REVIEWERS`: selects profiles and chain order; empty derives profiles from environment.
- `CLAUDEX_REVIEWER_<NAME>_BASE_URL`: directly controls destination; empty fails profile configuration.
- `..._API_KEY`: nonempty is refused; empty means no key.
- `..._API_KEY_ENV`: selects any environment variable without provider binding; empty means no key — finding 5.
- `..._MODEL`: required but otherwise remote-controlled model identifier.
- `..._TEMPERATURE`, `..._MAX_TOKENS`, `..._TIMEOUT`, `..._CONTEXT`: garbage fails configuration; negative or extremely large numeric values are not range-checked.
- `..._AUTOLOAD`: only `1/true/yes/on` enables it; empty and garbage disable it.
- `CLAUDEX_FALLBACK_MIN_FINDINGS`: garbage aborts when present in the process environment; zero or a negative number disables the rubber-stamp threshold.
- `CLAUDEX_ALLOWED_PATHS`: empty adds nothing; any valid path grants wrapper file access there — finding 9.
- `CLAUDEX_DISABLE_MCP`: unset disables only two named servers; empty disables none — finding 11.
- `CLAUDEX_CODEX_BIN`: any nonempty path to a file replaces Codex — finding 10.
- `PATH`: selects `codex`, `lms`, Bash, and Python executables without integrity verification.
- `HTTP_PROXY`, `HTTPS_PROXY`, `ALL_PROXY`, `NO_PROXY`: inherited by outbound clients; finding 1.
- Codex’s own configuration/authentication environment is inherited unchanged by `Popen`; finding 12.
- `PYTHONUTF8=1` affects encoding only and does not weaken a protection.

6. **MEDIUM — the review validity protection can be switched off numerically.**  
   Anchor: `scripts/fallback_review.py:657` — `default=int(os.environ.get("CLAUDEX_FALLBACK_MIN_FINDINGS", "3")),`  
   Anchor: `scripts/fallback_review.py:619` — `if args.round_no == 1 and verdict == "APPROVED" and findings < args.min_findings:`  
   Attack: `CLAUDEX_FALLBACK_MIN_FINDINGS=0`, `--min-findings 0`, or claiming `--round 2` disables the only anti-rubber-stamp test. This does not grant machine privilege, but it lets attacker-controlled content manufacture an accepted review.  
   Fix: enforce a positive minimum internally and derive the round from trusted state rather than caller input.

## 5. Secrets

API keys are placed in HTTP headers, not directly in the fallback process argv. They leave the process for preflight and review routes and can reach the wrong host through findings 1, 3, and 5. Codex credentials are managed by the child CLI, but its environment/configuration is inherited.

7. **HIGH — arbitrary files and unredacted tool output can be selected for remote exfiltration.**  
   Anchor: `scripts/fallback_review.py:507` — `system_prompt = read_text(args.system_file) if args.system_file else SYSTEM_PROMPT`  
   Anchor: `scripts/fallback_review.py:711` — `"responses — check whether they are addressed, and find what is "`  
   Anchor: `scripts/fallback_review.py:715` — `+ read_text(args.plan) + "\n=== END PLAN ===")`  
   Attack: `--plan`, `--log`, and `--system-file` have no root restriction or sensitive-file exclusion. An allowlisted or mistaken caller can select `.env`, credential files, scanner output, or arbitrary readable files and transmit them as prompt content. There is no secret redaction.  
   Fix: confine inputs to approved repository files, deny credential/config patterns, and run redaction before constructing any remote prompt.

8. **MEDIUM — provider-controlled error bodies are disclosed to logs.**  
   Anchor: `scripts/fallback_review.py:526` — `detail = exc.read().decode("utf-8", "replace")[:500]`  
   Anchor: `scripts/fallback_review.py:529` — `raise ProviderError(f"HTTP {exc.code} from {p['base_url']} — {detail}",`  
   Anchor: `scripts/fallback_review.py:749` — `print(f"{'CHAIN: ' if args.chain else 'ERROR: '}{name}: {exc} [{kind}]")`  
   Attack: a provider can reflect request fragments, credentials, tenant identifiers, or internal diagnostics into its error body, which is then printed into orchestration logs. Unexpected successful response shapes are similarly dumped at line 538.  
   Fix: log only status code and a generated request ID; store sanitized provider details only under an explicit debug option.

## 6. Input at the trust boundary

Shell injection into the child argv is prevented by list-form `Popen`. Model and resume identifiers are constrained. YAML uses `yaml.safe_load`, so unsafe YAML object construction was not found. Dotted forbidden keys are caught when exactly lowercase and passed through `-c`, but other input surfaces remain unsafe.

9. **CRITICAL — the allowlisted wrapper itself is an arbitrary overwrite/delete primitive.**  
   Anchor: `scripts/codex_ro.py:124` — `opt_ins = list(extra) + os.environ.get("CLAUDEX_ALLOWED_PATHS", "").split(os.pathsep)`  
   Anchor: `scripts/codex_ro.py:127` — `roots.append(Path(raw.strip()).expanduser().resolve())`  
   Anchor: `scripts/codex_ro.py:364` — `--allow-path",`  
   Anchor: `scripts/codex_ro.py:429` — `out_file.unlink()`  
   Anchor: `scripts/codex_ro.py:448` — `with open(stream_file, "wb") as stream_handle, open(err_file, "wb") as err_handle:`  
   Attack: the unattended caller can choose any repository or temp file as `--out-file`/`--err-file`, causing deletion or truncation. Supplying `--allow-path /`, a drive root, or `CLAUDEX_ALLOWED_PATHS` extends that primitive anywhere the wrapper user can write. This defeats the claim that accepted wrapper calls cannot obtain write access. A symlink/parent-directory race also exists between resolution and later open/unlink.  
   Fix: remove caller-controlled roots, use one administrator-created output directory outside the repository, open files without following links, and refuse existing targets.

10. **MEDIUM — prompt and review inputs have no size limits.**  
    Anchor: `scripts/fallback_review.py:476` — `with open(path, encoding="utf-8-sig", errors="replace") as fh:`  
    Anchor: `scripts/fallback_review.py:477` — `return fh.read()`  
    Anchor: `scripts/codex_ro.py:419` — `prompt = prompt_file.read_text(encoding="utf-8")`  
    Attack: a huge plan, log, system file, or prompt file is read wholly into memory, then serialized and potentially retried across multiple paid providers.  
    Fix: stat and stream-read with strict byte/token limits before allocating or sending.

## 7. Webhooks and callbacks

n/a — no webhook, callback, listener, or inbound route exists. `PreToolUse` is a local process hook, not a network callback.

## 8. Transport

Default HTTPS certificate verification is retained; no `ssl` override or disabled certificate check was found. Non-loopback textual hostnames require HTTPS. Findings 1, 3, and 4 defeat the intended transport boundary through proxies, redirects, and false loopback classification. Proxy environment is honored implicitly and not constrained.

## 9. Abuse and resource exhaustion

Retries are bounded to one preflight retry and two review attempts per provider. The provider chain itself is caller-configured and finite, but every configured provider can incur a preflight plus up to two paid review requests. Timeouts exist, although arbitrarily large values are accepted. Finding 10 covers oversized inputs; finding 6 covers disabling the review-quality gate.

## 10. Execution and sandbox escape

11. **CRITICAL — `CLAUDEX_CODEX_BIN` and `PATH` let the caller replace the protected program with arbitrary code.**  
    Anchor: `scripts/codex_ro.py:219` — `override = os.environ.get("CLAUDEX_CODEX_BIN")`  
    Anchor: `scripts/codex_ro.py:221` — `if not Path(override).expanduser().is_file():`  
    Anchor: `scripts/codex_ro.py:223` — `return str(Path(override).expanduser())`  
    Anchor: `scripts/codex_ro.py:450` — `[executable, *argv_child],`  
    Attack: the only validation is “is a file.” A malicious executable or script supplied through `CLAUDEX_CODEX_BIN`, or a PATH-shadowed `codex`, receives the prompt and executes with the wrapper’s full host privileges, completely outside Codex sandbox enforcement.  
    Fix: remove the environment override from allowlisted operation and execute a pinned absolute binary whose ownership and cryptographic hash are verified.

12. **CRITICAL — arbitrary Codex configuration can create or enable MCP command servers.**  
    Anchor: `scripts/codex_ro.py:185` — `for server in args.disable_mcp:`  
    Anchor: `scripts/codex_ro.py:186` — `argv += ["-c", f"mcp_servers.{server}.enabled=false"]`  
    Anchor: `scripts/codex_ro.py:187` — `for override in args.config:`  
    Anchor: `scripts/codex_ro.py:188` — `argv += ["-c", override]`  
    Anchor: `scripts/codex_ro.py:380` — `raw_mcp = os.environ.get("CLAUDEX_DISABLE_MCP")`  
    Attack: only two installation-specific MCP names are disabled by default, an empty variable disables none, and arbitrary `-c mcp_servers.<name>.*=...` overrides are accepted. A caller or repository/user Codex config can define a command-backed MCP server that writes files, accesses the network, or runs arbitrary host commands. The model can then call it despite the shell sandbox being read-only.  
    Fix: reject every `mcp_servers` override, launch with MCP globally disabled in an isolated configuration home, and allow no external tools in reviewer mode.

13. **HIGH — approval policy and inherited Codex configuration are not nailed shut.**  
    Anchor: `scripts/codex_ro.py:68` — `FORBIDDEN_CONFIG_KEYS = (`  
    Anchor: `scripts/codex_ro.py:70` — `"approval_policy",`  
    Anchor: `scripts/codex_ro.py:174` — `def build_argv(args: argparse.Namespace, out_file: Path) -> list[str]:`  
    Anchor: `scripts/codex_ro.py:449` — `proc = subprocess.Popen(`  
    Attack: the wrapper rejects a caller-provided `-c approval_policy=...`, but never supplies a safe approval policy itself and launches Codex with its inherited environment and normal repository/user configuration. A config file, default profile, `CODEX_HOME`, provider definition, or newly introduced equivalent key can therefore enable auto-approval or tools the wrapper never inspected. This also bypasses the lowercase forbidden-key check by moving the setting out of wrapper arguments rather than spelling it differently.  
    Fix: use an isolated empty configuration home and sanitized environment, explicitly set both sandbox and approval policy last, and reject all configuration except a small enumerated allowlist.

For direct `-c` values, no shell/argv injection was found: arguments remain separate list elements. Exact lowercase `sandbox_mode`, `approval_policy`, `sandbox_permissions`, `sandbox_workspace_write`, and their dotted children are refused. Case variants do not affect those case-sensitive Codex keys based on the shown configuration model. The practical bypasses are config files/profiles, MCP configuration, executable replacement, caller-controlled output roots, and inherited environment.

14. **HIGH — the wrapper guard fails open when its interpreter or payload handling fails.**  
    Anchor: `hooks/claudex-python.sh:41` — `echo "claudex-loop: no working Python 3.8+ found (tried: python3, python, py -3)." >&2`  
    Anchor: `hooks/claudex-python.sh:43` — `exit 1`  
    Anchor: `hooks/wrapper_guard.py:175` — `payload = json.loads(raw)`  
    Anchor: `hooks/wrapper_guard.py:177` — `return 0  # not our business; never block on unparseable input`  
    Attack: the hook’s documented blocking contract is exit 2, but the shim exits 1 when Python is absent or PATH-shadowed; malformed/truncated JSON explicitly exits 0. In either condition the allowlist remains while its compensating guard does not issue a deny, allowing `&&`, `;`, pipelines, or another command to ride along if the host treats non-blocking hook failures as errors rather than denials.  
    Fix: make every hook infrastructure/parsing failure involving the matched Bash/PowerShell tool exit 2 with an explicit deny, and use a pinned interpreter.

For a correctly delivered JSON command that begins with the documented literal wrapper invocation, I found no missed ordinary Bash/PowerShell separator: `;`, `&`, `&&`, pipes, redirections, parentheses, command substitution, backticks, and newlines are all denied. The remaining second-command exposure is loss or bypass of the guard itself, especially finding 14. PATH replacement of `python` can also execute arbitrary code before `wrapper_guard.py` or `codex_ro.py` runs.

## 11. Dependencies and copied wrappers

15. **HIGH — enforcement depends on unpinned ambient executables and drifting copies.**  
    Anchor: `scripts/codex_ro.py:8` — `WHY THE WRAPPER EXISTS (measured 2026-08-27, codex-cli 0.149.1; every row is a`  
    Anchor: `scripts/codex_ro.py:23` — `This file is the CANONICAL implementation. Repos receive a copy at`  
    Anchor: `scripts/codex_ro.py:24` — `` `tools/codex_ro.py`; `scripts/wrapper_drift.py` reports copies that fell behind.``  
    Anchor: `scripts/fallback_review.py:255` — `import yaml`  
    Attack: the security guarantee relies on version-specific Codex precedence and ambient PATH-installed Python, Bash, `lms`, Codex, and PyYAML. A reporting-only drift mechanism does not ensure deployed copies receive security fixes; an old copied wrapper can remain allowlisted indefinitely.  
    Fix: pin and verify the Codex/runtime versions, test supported versions continuously, and make installation/update fail when the deployed wrapper hash differs from the canonical signed copy.

## 12. Leakage

16. **MEDIUM — sensitive model output and diagnostic streams are persisted with ordinary filesystem permissions.**  
    Anchor: `scripts/codex_ro.py:425` — `stream_file = Path(str(out_file) + ".stream.json")`  
    Anchor: `scripts/codex_ro.py:448` — `with open(stream_file, "wb") as stream_handle, open(err_file, "wb") as err_handle:`  
    Anchor: `scripts/fallback_review.py:561` — `with open(args.out, "w", encoding="utf-8", newline="\n") as fh:`  
    Anchor: `scripts/fallback_review.py:572` — `with open(args.append_log, "a", encoding="utf-8", newline="\n") as fh:`  
    Attack: answers, event streams, provider diagnostics, and full critiques derived from sensitive source material are left in caller-selected repository or temp paths using inherited umask/ACLs. Paths and model/profile identities are also printed. Shared temp or repository users may read these artifacts.  
    Fix: create artifacts in a dedicated private directory with restrictive permissions, sanitize diagnostics, and define retention/deletion behavior.

No direct API-key logging was found in the normal success path, and fallback keys are not placed in argv. However, redirects, provider error reflection, arbitrary input selection, inherited Codex configuration, and persistent output files prevent treating secrets as contained.

Checked: 1-12, n/a: inbound portions of 2 and 3 because there is no inbound route; 7 because there are no webhooks or callbacks
EXPOSURE: UNSAFE
---

## Verification performed by Claude before triage

Findings a cold reader raises are not tasks until they are checked against the code.
These are the checks that were actually run; the outcome of each is carried into the
baseline's triage column.

| Claim | Check | Outcome |
|---|---|---|
| S4-1 `${IFS}` guard bypass | fed the guard `{"tool_input":{"command":"python tools/codex_ro.py${IFS}&&whoami"}}` | **exit 0 (allowed)**; control `... && whoami` → exit 2. CONFIRMED |
| S4-1, second half | ran `python probe_first.py${IFS}&&echo ...` in this bash | both halves executed. CONFIRMED |
| S4-1 test coverage | `grep -c IFS tests/test_wrapper_guard.py` | `0`. CONFIRMED |
| S1-1 / EXP-9 write primitive | read `codex_ro.py:124-127, 364, 427-429, 448` | `--allow-path` is a plain flag, so it rides the allowlist prefix; `out_file.unlink()` and `open(err_file,"wb")` follow. CONFIRMED |
| S1-3 / EXP-12 MCP override | read `build_argv` (`codex_ro.py:185-189`) and `FORBIDDEN_CONFIG_KEYS` (`:68-73`) | user `-c` is appended *after* the disable overrides and `mcp_servers` is not forbidden. CONFIRMED |
| MCP_DOCKER default | ran the wrapper with defaults, then with `--disable-mcp n8n` | default → codex exits 1, `invalid transport in mcp_servers.MCP_DOCKER`; with `n8n` only → `PROBE OK`. CONFIRMED, contradicts `codex_ro.py:61` |
| S2-1 gates disabled by config | planted `.claudex.yaml` with `producer_never_reviews:false`, `adversary_read_only:false`, `sandbox: danger-full-access` | `--explain` → **"gates OK", exit 0**; `--spec audit` → `sandbox=danger-full-access`. CONFIRMED — but the `write_access` half of the claim is **wrong**: that gate still fired when tested separately |
| S2-2 `cross` with one author | planted `roles: {plan: [claude], plan-review: cross}` | `plan claude -> plan-review claude (cross-check)`, `producer_never_reviews : True`, **"gates OK"**. CONFIRMED |
| S2-4 uncaught traceback | planted a config with `roles:` as a list | `AttributeError` traceback, **exit 1** (not the documented refusal code 2). CONFIRMED |
| S3-Sec1 / EXP-2 egress default-open | read `check_egress` (`fallback_review.py:300-360`) | with no allowlist file and `CLAUDEX_EGRESS_ALLOW` unset, any HTTPS host passes; the docstring says so at `:313`. CONFIRMED |
| S3-Sec2 / EXP-3 redirects | read `http_get_json` (`:369-373`) and the chat call (`:520-523`) | plain `urllib.request.urlopen`, default redirect handler, `check_egress` only on the first URL. CONFIRMED |
| S3-Q2 `lms` timeout | read `:404` and `:411-413` | both `subprocess.run` calls have no `timeout=`. CONFIRMED |
| S3-Q5 usage cutoff | read `codex_usage.py:34` | `for path in files[:10]:`. CONFIRMED |
| S3-Rules1 `--append-log` | read `:638` | optional argument. CONFIRMED |
| EXP-14 shim fails open | read `hooks/claudex-python.sh:41-43` and `hooks.json` | no Python → `exit 1`; a PreToolUse hook must exit 2 to deny, so the call proceeds unguarded. CONFIRMED |
| S5a-1 / S6-5 `REVIEW_PROMPT` | `grep -rn REVIEW_PROMPT skills/` | `claudex-loop:230` and `plan-review:134` both `cat` a file no step creates. CONFIRMED |
| S5a-2 / S5a-10 "wrong wrapper path" | read `codex_ro.py:23-24` and `README.md:161` | **REJECTED** — `scripts/` is the canonical copy in *this* repo, `tools/` is where target repos receive it, which is where the skills run |
| S5b-2 direct `codex exec` | read `plan-review:132,150`, `setup:283` vs `docs/betrieb.md:51` | the rule "Auch Ping und Resume laufen über den Wrapper" is stated verbatim and broken. CONFIRMED |
| S5b-5 plan inlining | read `plan-review:127` vs `docs/betrieb.md:168` | rule says inline the plan text, skill says read `PLAN.md`. CONFIRMED |
| S6 legacy reachability | read `.claude-plugin/plugin.json`, `marketplace.json`, `README.md:161` | `legacy/` is **not** registered as skills and is documented as superseded — severity reduced accordingly |

### Process note — a leak this audit caused

While chasing the `MCP_DOCKER` failure, the full contents of `~/.codex/config.toml` were
printed into the session transcript, including a plaintext `Authorization` bearer token
for the local n8n MCP server. That violates the standing rule to never emit match lines
from credential-bearing files (`-l` / key names only). The value is not repeated here or
in the baseline. It is the user's machine config, outside this repo, and gitleaks found
nothing in the repo itself — but the token should be treated as exposed to this
transcript and rotated if the transcript is retained.
